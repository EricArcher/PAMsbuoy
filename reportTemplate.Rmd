---
title: "`r title`"
params:
  set_title: temp title
output:
  pdf_document: default
  html_document: default
header-includes:
  - \usepackage{booktabs}
  - \usepackage{longtable}
  - \usepackage{array}
  - \usepackage{multirow}
  - \usepackage[table]{xcolor}
  - \usepackage{wrapfig}
  - \usepackage{float}
  - \usepackage{colortbl}
  - \usepackage{pdflscape}
  - \usepackage{tabu}
  - \usepackage{threeparttable}
  - \usepackage{underscore}
---


```{r echo=FALSE}
detSummary <- detSummary %>%
  mutate(StationNum = as.numeric(as.factor(Station)),
         KSpecies=paste0(Species, ' <b>[', UniqueDetections,']</b>'),
         KBuoy=paste0(Buoy,' <b>[', NumDetections,']</b>')) %>%
  head(tempRows)
odds <- which(detSummary$StationNum %% 2 == 1)
detSummary <- select(detSummary, -StationNum) %>%
  select(Station, KSpecies, KBuoy, Latitude, Longitude, UTC)
myColumns <- c('Station', 'Species<br/>[Unique Detections]', 'Buoy<br/>[Detections]',
               'Latitude', 'Longitude', 'UTC')
kable(detSummary)
# kable(detSummary,  align='c', digits=2,
#       col.names=myColumns, escape=FALSE) %>%
#   kable_styling('bordered') %>%
#   row_spec(odds, background='#edf0f4') %>%
#   collapse_rows(which(colnames(detSummary) %in% c('KSpecies','Station')))



# detSummary <- detSummary %>% 
#   mutate(StationNum = as.numeric(as.factor(Station))) %>% 
#   head(tempRows)
# odds <- which(detSummary$StationNum %% 2 == 1)
# detSummary <- select(detSummary, -StationNum) %>% 
#   select(Station, Species, UniqueDetections, Buoy, NumDetections, Latitude, Longitude, UTC)
# kable(detSummary, format='html', align='c', digits=2) %>% 
#   kable_styling('bordered') %>% 
#   row_spec(odds, background='#edf0f4') %>% 
#   collapse_rows(which(colnames(detSummary) %in% c('Species','Station')))



include_graphics('./Report/detectionPlot.jpeg')
# detectionPlotCombined
```